name: my_action
run-name: ${{github.actor}} runs the workflow.
on: [push]
jobs:
    pipo-job:
        runs-on: ubuntu-latest
        steps:
	    - name: First step.
              uses: actions/checkout
